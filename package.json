{
  "name": "battery-ble",
  "version": "1.0.0",
  "description": "Communication with Ultimatron battery BMS",
  "main": "src/index.ts",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start": "npx nodemon",
    "start:prod": "npm run build && node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "bms",
    "ultimatron",
    "battery"
  ],
  "author": "sergiy.khruschak@gmail.com",
  "license": "MIT",
  "dependencies": {
    "node-ble": "^1.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.5.6",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
